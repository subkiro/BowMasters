using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FollowTheArrow : MonoBehaviour
{
    // Start is called before the first frame update
    Transform arrow;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
